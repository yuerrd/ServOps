# Docker 跨节点部署环境变量配置
# 复制此文件为 .env 并根据部署场景修改值

# ===========================================
# 场景1：前端和后端在同一节点 (默认配置)
# ===========================================
# BACKEND_HOST=http://backend:3001

# ===========================================
# 场景2：前端和后端在不同节点
# ===========================================
# 前端节点配置 - 指向后端节点的公网IP或域名
BACKEND_HOST=http://192.168.1.100:3001
# 或使用域名
# BACKEND_HOST=https://api.servops.yourdomain.com

# 后端节点配置 - 允许前端跨域访问
CORS_ORIGIN=http://192.168.1.101
# 或使用域名
# CORS_ORIGIN=https://servops.yourdomain.com

# ===========================================
# 数据库配置（外部 MySQL 服务）
# ===========================================
DB_HOST=192.168.1.200
DB_PORT=3306
DB_USER=servops_user
DB_PASSWORD=your_strong_password
DB_NAME=servops

# ===========================================
# Redis 配置（外部 Redis 服务，可选）
# ===========================================
REDIS_HOST=192.168.1.201
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# ===========================================
# 应用配置
# ===========================================
NODE_ENV=production
PORT=3001
SSH_TIMEOUT=30000

# ===========================================
# 安全配置（生产环境必须修改）
# ===========================================
JWT_SECRET=your_jwt_secret_here_change_in_production
ENCRYPTION_KEY=your_32_char_encryption_key_here!