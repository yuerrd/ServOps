version: '3.8'

services:
  # 后端 API 服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: servops-backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3001
      DB_HOST: ${DB_HOST:-localhost}
      DB_PORT: ${DB_PORT:-3306}
      DB_USER: ${DB_USER:-servops_user}
      DB_PASSWORD: ${DB_PASSWORD:-servops_pass_2024}
      DB_NAME: ${DB_NAME:-servops}
      DEFAULT_SSH_TIMEOUT: ${SSH_TIMEOUT:-30000}
      REDIS_HOST: ${REDIS_HOST:-localhost}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      # CORS 配置，支持跨节点部署
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
    volumes:
      # 可选：挂载 SSH 私钥目录（如果需要）
      - ssh_keys:/app/ssh-keys:ro
    ports:
      - "3001:3001"
    networks:
      - servops-network
    profiles:
      - backend
      - all
    healthcheck:
      test: ["CMD", "node", "healthcheck.js"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # 前端 Web 服务
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # 构建时传入后端地址
        REACT_APP_API_BASE_URL: ${BACKEND_HOST:-http://backend:3001}
        REACT_APP_SOCKET_URL: ${BACKEND_HOST:-http://backend:3001}
    container_name: servops-frontend
    restart: unless-stopped
    environment:
      # 运行时环境变量（用于 Nginx 配置）
      BACKEND_HOST: ${BACKEND_HOST:-backend:3001}
    ports:
      - "80:80"
      - "443:443"
    networks:
      - servops-network
    profiles:
      - frontend
      - all
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  ssh_keys:
    driver: local

networks:
  servops-network:
    driver: bridge